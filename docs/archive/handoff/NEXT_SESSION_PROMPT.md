# 下一次对话提示词

请先完整阅读本交接包中的 `README_FIRST.md` 和 `HANDOFF.md`，再检查源码。不要沿用更早交接包中“CH9350 尚未接入”或“RTC 每次按编译时间重设”的旧结论。

当前工程：

```text
E:\STM32Project\STM32H743_5inch_GT911_Test
Keil: Projects\MDK-ARM\atk_h743.uvprojx
Target: TOUCH
Arm Compiler 6.24
```

当前关键状态：

1. CH9350 USB 鼠标已经接入 USART1，用户已确认鼠标可以使用。
2. 当前解析下位机状态 2 的 7 字节相对鼠标帧：`57 AB 02 Button X Y Wheel`。
3. 已实现移动、左键、左键拖动、右键返回，以及 FILES/LOGS 的滚轮翻页；触摸和鼠标共存。
4. DS3231 原先每次换新固件都按 `__DATE__/__TIME__` 覆盖时间，导致出现固定慢十几分钟。现在只在时间无效或 OSF 置位时使用编译时间初始化。
5. SETTINGS 新增 TIME 页面，可设置年月日时分秒并写入 DS3231。RTC 改动已编译通过，但尚未收到用户的实物复测确认。
6. 不要修改用户未要求的其他功能；任何修改后都要执行 Keil 全量 Rebuild 并报告错误、警告、程序尺寸、HEX 时间戳和 SHA-256。
7. 工程不是 Git 仓库，不得用 Git 命令回滚用户文件。

当前基础考核最明确的剩余点：CH9350 断开/恢复状态还没有接入桌面提示和日志；需要补齐考核规定的 60 秒高频输入、前后台并发、资源上限、60 分钟运行和异常注入测试记录；尚未使用 Git 管理。除非用户明确要求，先分析方案，不要擅自扩展功能。
